{
  "data": {
    "memory_rules": {
    "stages": ["hardware_repair", "hardware_tuning"],
      "guidance": [
        "If memory allows, prioritize adjusting the model's topology — reducing the degree of parallelism can significantly improve throughput.",
        "If vLLM has no pending requests but KV-cache utilization is low, consider reducing the tensor-parallel (TP) and pipeline-parallel (PP) sizes."
      ]
    }
  }
}
